# Copy this file to ".env" and fill in your key. ".env" is git-ignored.
# Only needed for prelabel.py (pre-labeling). collect.py needs no credentials.

# Groq API key (https://console.groq.com/keys) — used for pre-labeling.
GROQ_API_KEY=your_key_here
